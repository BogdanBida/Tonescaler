/* tslint:disable:no-unused-variable */

import { StagecolorPipe } from './stagecolor.pipe';

describe('Pipe: Notecolore', () => {
  it('create an instance', () => {
    let pipe = new StagecolorPipe();
    expect(pipe).toBeTruthy();
  });
});
